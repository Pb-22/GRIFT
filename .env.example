# Copy to .env and fill in (never commit .env)
#   cp .env.example .env

# Required for --cron / reliable rate limits (optional but recommended interactively)
GITHUB_TOKEN=

# Optional: only needed if using --submit-triage later
TRIAGE_KEY=

# Optional: abuse.ch / ThreatFox enrichment (future)
ABUSECH_AUTH_KEY=
